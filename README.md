# bundle

This branch contains **generated output** — do not hand-edit.

It is assembled automatically by `scripts/build-bundle.mjs` in the superproject (`main` branch).
Every commit here is produced by the build script; manual edits will be overwritten on the next run.
